name: Trigger Domain Grid Workflow

on:
  workflow_dispatch:

jobs:
  trigger-domain-grid:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger downstream workflow via GitHub API
        run: |
          echo "🚀 Dispatching workflow in domain grid repo..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GIT_PAT }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/Dmitrii-Zavalin-Deployments/engineering_simulations_pipeline_domain_grid_sred/actions/workflows/fluid_simulation_pipeline.yml/dispatches \
            -d '{"ref":"main"}'



